services:
  backend:
    build: .
    container_name: backend
    command: "python manage.py runserver 0.0.0.0:8000"
    volumes:
      - ./core:/app
    ports:
      - "8000:8000"
    environment:
      - SECRET_KEY=@5wofme%bm0ain2ous2x2$&i(@pvl7we==wn)#rm1k3!((-5($
      - DEBUG=True
      - ALLOWED_HOSTS=*
    
      
   